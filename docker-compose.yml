services:
  goodbuzz:
    build: .
    container_name: goodbuzz
    restart: unless-stopped

    ports:
      - "8080:8080"

    environment:
      GOODBUZZ_PORT: "8080"

    volumes:
      - ./data:/app/data